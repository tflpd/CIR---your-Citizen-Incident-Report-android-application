<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.inducesmile.citizenreportingtool">
    
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_landmark_solid"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true">

        <activity android:name=".IntroActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
        </activity>

        <activity android:name=".PhotoPreviewActivity"
            android:label="Show the image taken"/>

        <activity android:name=".CreateReportActivity"
            android:label="Create the report to be sent"/>

        <activity android:name=".MenuActivity"
            android:label="Show the application's menu"/>

        <activity android:name=".MyAccountActivity"
            android:label="Show the user's details"/>
        
        <activity android:name=".MyReportsActivity"
            android:label="Show user's earlier reports"/>
        <activity android:name=".MySettingsActivity"
            android:label="Show user's current settings"/>
        <activity android:name=".LogInActivity"
            android:label="User's log in page"/>
        <activity android:name=".SignUpActivity"
            android:label="User's sign up page"/>

    </application>

</manifest>